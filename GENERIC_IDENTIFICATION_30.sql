CREATE TABLE GENERIC_IDENTIFICATION_30 (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    IDENTIFICATION VARCHAR2(4) CHECK ( LENGTH (IDENTIFICATION) = 4 ) NOT NULL,
    ISSUER VARCHAR2(35) NOT NULL,
    SCHEME_NAME VARCHAR2(35),
    CONSTRAINT PK_GENERIC_IDENTIFICATION_30 PRIMARY KEY (ID)
);

